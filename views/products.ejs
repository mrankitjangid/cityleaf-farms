<section class="p-2 md:p-8 flex flex-col items-center">
    <div class="text-4xl font-bold text-center">Products</div>
    <div class="products-container flex flex-wrap flex-grow justify-around gap-4 lg:gap-8 p-4 lg:p-8 max-w-[1200px]">

        <% for (let i = 0; i < data.length; i++) { %>
            
            <div class="product max-w-72 rounded-3xl overflow-hidden border-2 border-white font-bold">
                <div class="">
                    <img src="<%= data[i].img %>" alt="product">
                    <!-- <div class="relative -mt-10 w-full content-center flex justify-center">
                        <button class="bg-white rounded-t-xl px-4 py-2 hover:bg-yellow-500" type="button" onclick="addItem(JSON.stringify('<%= data[i]._id %>'))">ADD TO CART</button>
                    </div> -->
                </div>
                <div class="flex justify-between p-4">
                    <span class=""><%= data[i].name %></span>
                    <span class="">$<%= data[i].price %></span>
                </div>
            </div>

        <% } %>

    </div>
</section>